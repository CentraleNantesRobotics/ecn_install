# /etc/nslcd.conf
# nslcd configuration file. See nslcd.conf(5)
# for details.

# The user and group nslcd should run as.
uid nslcd
gid nslcd

# The location at which the LDAP server(s) should be reachable.
uri ldap://rldap.ec-nantes.fr

# The search base that will be used for all queries.
base passwd ou=people, dc=ec-nantes, dc=fr
base dc=ec-nantes,dc=fr

# The LDAP protocol version to use.
#ldap_version 3

# The DN to bind with for normal lookups.
binddn cn=pferldap,dc=ec-nantes,dc=fr
bindpw Luxybox1606

# The DN used for password modifications by root.
#rootpwmoddn cn=admin,dc=example,dc=com

# SSL options
#ssl off
#tls_reqcert never
tls_cacertfile /etc/ssl/certs/ca-certificates.crt

# The search scope.
#scope sub

#filtre pour refuser les comptes
filter passwd (!(CentraleAccessRightsDisallow=*pfe*))
